rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;  // 允許所有人讀取
      allow write: if request.auth != null;  // 只允許已登入用戶寫入
    }
  }
} 